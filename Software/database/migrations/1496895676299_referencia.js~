'use strict'

const Schema = use('Schema')

class ReferenciaTableSchema extends Schema {

  up () {
    this.create('referencia', (table) => {
      table.increments()
      table.timestamps()
      table.integer('id_catalogo') /* foranean key*/
      table.string('url')	
    })
  }

  down () {
    this.drop('referencia')
  }

}

module.exports = ReferenciaTableSchema
